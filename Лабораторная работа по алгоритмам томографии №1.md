# Отчет по лабораторной работе №1

## 1. Титульный лист
* **Название работы:** Генерация синтетических томографических изображений (фантомов)
* **Дисциплина:** Алгоритмы томографии
* **Студент:** [Перунов Илья Романович]
* **Группа:** [13.2]

## 2. Листинг программы
Ниже представлен код на языке Python.

```python
import numpy as np
import matplotlib.pyplot as plt

def generate_phantom(M, N, delta, K, mu_range, R_range, sigma):
    # Шаг 1: Создание координатной сетки
    j_idx = np.arange(1, N + 1)
    i_idx = np.arange(1, M + 1)
    x = (j_idx - (N + 1) / 2) * delta
    y = ((M + 1) / 2 - i_idx) * delta
    X, Y = np.meshgrid(x, y)
    
    # Шаг 2: Инициализация томограммы
    P = np.zeros((M, N))
    
    np.random.seed(42) # Фиксация seed для воспроизводимости
    
    # Шаг 3: Добавление окружностей
    for k in range(K):
        mu_k = np.random.uniform(mu_range[0], mu_range[1])
        R_k = np.random.uniform(R_range[0], R_range[1])
        x_k = (np.random.rand() - 0.5) * N * delta
        y_k = (np.random.rand() - 0.5) * M * delta
        
        d_sq = (X - x_k)**2 + (Y - y_k)**2
        mask = (d_sq <= R_k**2).astype(float)
        P += mu_k * mask
        
    # Шаг 4: Сглаживание (если sigma > 0)
    if sigma > 0:
        L = int(np.ceil(3 * sigma / delta))
        p = np.arange(-L, L + 1)
        h = np.exp(-(p * delta)**2 / (2 * sigma**2)) / (np.sqrt(2 * np.pi) * sigma)
        h /= np.sum(h) # Нормировка ядра
        
        P_temp = np.zeros_like(P)
        P_smooth = np.zeros_like(P)
        
        # Свертка по строкам с дополнением нулями (zero padding)
        for i in range(M):
            for j in range(N):
                val = 0
                for p_idx, p_val in enumerate(p):
                    col = j + p_val
                    if 0 <= col < N:
                        val += P[i, col] * h[p_idx]
                P_temp[i, j] = val
                
        # Свертка по столбцам
        for i in range(M):
            for j in range(N):
                val = 0
                for q_idx, q_val in enumerate(p):
                    row = i + q_val
                    if 0 <= row < M:
                        val += P_temp[row, j] * h[q_idx]
                P_smooth[i, j] = val
        P = P_smooth
        
    return P, X, Y

def plot_results(P, X, Y, title):
    fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 5))
    
    # Томограмма
    im = ax1.imshow(P, extent=[X.min(), X.max(), Y.min(), Y.max()], origin='lower', cmap='bone')
    ax1.axhline(0, color='r', linestyle='--', alpha=0.5)
    ax1.axvline(0, color='r', linestyle='--', alpha=0.5)
    ax1.set_title(f'Томограмма\n{title}')
    ax1.set_xlabel('x, мм')
    ax1.set_ylabel('y, мм')
    fig.colorbar(im, ax=ax1, label='Коэф. ослабления $\mu$') 
    
    # Профиль по центру
    mid_row = P.shape[0] // 2
    ax2.plot(X[mid_row, :], P[mid_row, :])
    ax2.set_title('Профиль через центр')
    ax2.set_xlabel('x, мм')
    ax2.set_ylabel('Значение $\mu$')
    ax2.grid(True)
    
    plt.tight_layout()
    plt.show()
    
    # --- Эксперименты --- 
    mu_range = [0.1, 0.5] 
    R_range = [5, 30] K = 20 
    
    # Эксперимент 1 
    P1, X1, Y1 = generate_phantom(256, 256, 0.5, K, mu_range, R_range, 0)
    plot_results(P1, X1, Y1, r'$M=256, \Delta=0.5, \sigma=0$') 
    
    # Эксперимент 2 
    P2, X2, Y2 = generate_phantom(256, 256, 0.5, K, mu_range, R_range, 1.0) 
    plot_results(P2, X2, Y2, r'$M=256, \Delta=0.5, \sigma=1.0$') 
    
    # Эксперимент 3 
    P3, X3, Y3 = generate_phantom(512, 512, 0.25, K, mu_range, R_range, 0.5) 
    plot_results(P3, X3, Y3, r'$M=512, \Delta=0.25, \sigma=0.5$')
    
    # Исследование влияния параметра сглаживания (sigma = 0, 0.3, 1.0, 2.0)

	for sig in [0, 0.3, 1.0, 2.0]:
	P_exp, X_exp, Y_exp = generate_phantom(256, 256, 0.5, K, mu_range, R_range, 
	sig)
	plot_results(P_exp, X_exp, Y_exp, f'$\sigma={sig}$')
```

## 3.  Эксперименты 
### Эксперимент 1
Матрица (256 х 256), $\Delta  = 0.5$, $\sigma = 0$
![[Pasted image 20260319180659.png]]


### Эксперимент 2
Матрица (256 х 256), $\Delta  = 0.5$, $\sigma = 1.0$
![[Pasted image 20260319180724.png]]

### Эксперимент 3
Матрица (512 х 512), $\Delta  = 0.25, $\sigma = 0.5$
![[Pasted image 20260319181211.png]]

## Исследование влияния параметра сглаживания $\sigma \in {0, 0.3, 1.0, 2.0}$ на вид томограммы и профиля

![[Pasted image 20260319181629.png]]
![[Pasted image 20260319181635.png]]
![[Pasted image 20260319181640.png]]
![[Pasted image 20260319181644.png]]

